[DEFAULT]
cleaner = fakeroot debian/rules clean

[buildpackage]
upstream-tag = upstream-tarball/v%(version)s
debian-branch = debian-tarball/sid

[dch]
upstream-tag = upstream-tarball/v%(version)s
debian-branch = debian-tarball/sid

[import-orig]
pristine-tar = True
upstream-branch = upstream-tarball
upstream-vcs-tag = v%(version)s
upstream-tag = upstream-tarball/v%(version)s
debian-branch = debian-tarball/sid
merge-mode = replace
